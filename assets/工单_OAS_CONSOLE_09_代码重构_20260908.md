# 工单：OAS-CONSOLE-09 阶段A 代码重构（止血：拆 God file + 前端抽离 + 孤儿归档 + 二进制出库）【v1.7 七审修订版】

**窗口**：OAS 底座（oas-backend，窗口 7679323618953855019）
**日期**：2026-09-08（v1.7 修订 2026-09-08）
**优先级**：P0（结构性技术债，纯机械拆分，不动逻辑）
**依据**：OAS 第三方代码体检（2026-09-08）+ 四轮工单评审共 39 条意见（均已吸收；v1.4 补齐 middleware/state 归属、测试防线、快照脱敏、出口标准、部署流程）+ 主 Agent 五审（v1.5 补齐：08 时序前置、禁用账号口径统一、main.go 规模核对、线上全量复测、构建链路实跑、中间部署验证）+ 主 Agent 六审（v1.6 补齐：每步可编译纪律、双回滚锚点、断言冲突裁决、build.sh 前提、验收节点归属）+ 主 Agent 七审（v1.7 补齐：锚点 2 实体化 tag、快照接口清单写死、最终部署前 dry-run、08 暂停开工确认、seed 依赖注入口径）
**关联**：OAS-CONSOLE-08（管理员身份收窄，**时序依赖见下**）

## 背景
- OAS 线上服务 = `ziway-backend/cmd/oas/main.go`（7145 行），一个文件承载：全部顶层 model struct + 全部路由注册 + 全部 handler（内联闭包）+ 中间件 + 配置加载 + seed 逻辑 + 14 个前端页面 HTML 模板（Go 字符串拼接）+ 权限判断函数。
- 仓库另有未上线模块：`services/ams/`（第二套标准分层实现，独立 go.mod，未被引用）、`internal/mbs/`（12 模块）+ `internal/bos/`（13 模块，cmd/ms、cmd/os 未部署）、`dist/`（3 个预编译二进制共 68MB 入库，仓库 397MB）。
- 本次只做**阶段 A 止血**：拆文件、抽资源、归档孤儿、清二进制。阶段 B/C（ms/os 去留、数据库正名、权限收敛、能力升级）另行拍板，不入本单。
- **回滚预案（双锚点实体化，v1.7 修正）**：
  - **锚点 1（中间部署前）**：当前线上/仓库 HEAD = **f7d48aff59**（09-08 20:35）。窗口自测、A1/A2 阶段任何异常 → 回滚到 f7d48aff59 重新部署，不得在异常态继续叠加修改。
  - **锚点 2（中间部署验证通过后）**：中间部署验证（⑥）通过时，主 Agent **立即给该 commit 打 tag `oas-09-mid-verified`（或记录 hash）**，它就是锚点 2 的唯一回滚目标。A3/A4 阶段异常 → 回滚主分支到 `oas-09-mid-verified`（含拆分成果；A3 归档可逆、A4 `git rm --cached` 可恢复），**线上不回退**（中间部署已验证零变化、线上稳定）；仅当线上出现拆分引入的异常时才回滚 f7d48aff59。
  - **A0 基线产物先推独立分支 `refactor/oas-baseline`（见 A0）**，任何锚点回滚后均可从该分支恢复基线。

## 与 OAS-CONSOLE-08 的时序（P0，09 开工前置，v1.5 主 Agent 裁定）
- **裁定：09 先行，08 暂停待拆**。理由：08（管理员收窄）与 09（拆分）都改 main.go，并发必然冲突；08 当前仍处"待设计回报"阶段，未动工，切换零成本。
- **执行**：09 开工前，主 Agent 通知窗口 **08 暂停实施（仅保留设计回报）**；09 拆分完成并部署后，08 在**结构化代码上**继续实施（08 验收标准含的 `oas-ou-owner` seed 等由 08 在拆分后落地，不在 09 范围）。
- 若用户改判 08 先行：则 09 基线/回滚点随 08 部署后的新 HEAD 后移，本单所有 f7d48aff59 引用同步替换——**以主 Agent 最终指示为准**。

## 执行顺序（写死）
**① 开工检查 → ② A0 → ③ A1 → ④ A2 → ⑤ 窗口自测 → ⑥ 主 Agent 中间部署+线上复测（A1+A2 纯拆分零变化验证，通过即打 tag `oas-09-mid-verified`）→ ⑦ A3 → ⑧ A4 → ⑨ 主 Agent 最终部署（前置：部署环境新构建路径 dry-run 通过）→ ⑩ 线上复测 → 关单**
A3 与 A4 互不依赖，确认后可与自测并行推进；但**任何一步未过对应验收不得进入下一步**。部署权归主 Agent：窗口推完即回报，主 Agent 部署并线上复测，全部通过才关单。

**① 开工检查（强制）**：
- `git status` 确认窗口工作区干净；**有未提交改动必须先提交或 stash，并向主 Agent 说明**，再开始拆分——防止拆分 push 与窗口本地内容冲突/冲掉未提交内容。
- **08 暂停确认（v1.7）**：窗口回报"**已收到 08 暂停指示、无 08 相关未提交改动**"，主 Agent 确认后 09 才允许开工——08 仍改 main.go 时 09 开工必冲突。

## 需求范围

### A0 拆分前基线固化（先做，验收对照的唯一基准）
动手拆分前，先导出并留存以下基线（存入 `docs/oas-refactor-baseline/` 并提交）：
- **全量路由清单**：用 gin 的 **`engine.Routes()`** 导出（方法 + 路径 + 分组），存为 `routes-before.txt`；
- **关键接口响应快照**（`api-snapshots-before/`）。**接口清单写死（v1.7）**，14 个资源域逐域打钩，缺一域不验收：
  - health / OIDC discovery / JWKS / login / oauth token（幂等确定性接口）；
  - dashboard / approvals / ownership / admin-accounts / system-config / services / api-keys / federation-nodes / oauth-clients / audit-logs / rbac / users / roles / orgs —— **每域至少 1 个 GET（admin 登录态）**；
  - **含权限差异的域（audit-logs、oauth-clients）必须补 OAM 账号 403 用例**；
  - 禁用账号登录 401 用例（见下"禁用账号场景准备"）。
  - **登录态获取（二选一，回报中说明）**：① 真实账号密码（admin/test123，**如线上非此密码由主 Agent 在 A0 前提供**）登录拿 JWT 调 admin 接口，或 ② preview 域（BETA 档，quick-login/test-accounts 可用）生成快照；两者口径要一致，不能混用。
  - **快照脱敏（强制）**：token、密码、密钥类字段一律打码截断（如 `eyJ...****`），**禁止把有效 JWT/凭证原文 commit 进仓库**；统一优先使用 preview 域短时效 token，提交前整体检查一遍；
  - **快照比对口径（写死）**：**结构比对**（字段/类型/嵌套层级）逐接口对；**值比对**只对幂等确定性接口（health / discovery / jwks / token）；业务列表类接口（audit-logs / users / api-keys）**只对结构不对值**（本地 DB 数据与线上天然不同）；
  - **禁用账号场景准备（v1.6）**：验收 #5 需抓"禁用账号登录"拆分前快照，A0 阶段先确认测试数据存在禁用账号；无现成禁用账号 → 用 admin 账号 disable → 登录抓快照 → 抓完恢复，快照中注明。
- **14 个页面渲染截图**（`pages-before/`，登录态 + 非登录态）；
- **页面注入变量盘点**：逐页列出运行时注入的占位符清单，存为 `inject-vars-before.txt`（A2 执行与验收对照用，防漏替换）；
- **依赖盘点**：遍历 main.go 中 handler 闭包实际引用的依赖（DB/JWT/Log/Cfg/cache/kafka/eventbus/其他），存为 `deps-inventory.txt`，**Handlers struct 字段以盘点结果为准，不拍脑袋**；
- **共享可变状态盘点**：God file 中 package 级变量（缓存 map、token 黑名单、JWT secret 加载结果、内存配置副本等）逐一列出，存为 `shared-state-inventory.txt`（A1 拆到 `internal/oas/state.go` 用）；
- **authz 依赖来源确认**：确认 isAdminAccount / canOperateAdminAccount / isInAdminWhitelistA/B / canAccessOrgManagement 是纯 JWT claims 判断还是查 DB（如查 OU 关系），记入回报；查库者在 A1 中沿用依赖注入原则（见 A1）；
- **seed 执行时机记录**：确认 seed 是每次启动都跑、还是条件执行（如表空才 seed），记入 `seed-behavior.txt`（A1 拆分后保持同一时机，验收 #7 对照）；
- **编译基线**：`go build ./...` + `go vet ./...` + `go test ./...` 的原始输出。
- **A0 产物提交规范**：A0 基线**单独 commit**，并**先行推送到独立分支 `refactor/oas-baseline`**（主分支也保留），确保异常回滚后仍可从该分支恢复基线。
> 无基线不验收：拆完后的"零变化"全部以此 A0 产物为对照，而不是凭记忆。

### A1 拆分 cmd/oas/main.go（7145 行 → 职责化文件）【v1.3 修正包结构消除循环依赖 / v1.4 补 middleware/state 归属 / v1.6 补每步可编译纪律】
> **包结构**：`routes.go`（package oas）需要挂载 handlers，而 handlers 需要 models/authz —— models/authz 与 routes 同包会 **Go 循环导入（编译失败）**。models 与 authz 必须下沉独立子包，依赖方向固定为：`cmd/oas → internal/oas/routes → internal/oas/handlers → internal/oas/model + internal/oas/authz → pkg → 标准库`，**禁止反向引用、禁止同包混放**。

| 目标文件 | 承载内容 |
|---|---|
| `internal/oas/model/model.go` | 全部顶层 model struct（package model，以代码实际为准，约 13-14 个：SystemConfig/AuditLog/DomainRegistry/GovernancePolicy/ServiceRegistry/APIKey/FederationNode/OAuthClient/OAuthAuthorizationCode/RBACPolicy/OASUser/OASRole/OASUserRole 等，TableName() 方法随 struct 走）；**handler 内部使用的临时响应 struct 留在各 handler 文件内，不抽** |
| `internal/oas/authz/authz.go` | 权限判断函数（package authz）：isAdminAccount / canOperateAdminAccount / isInAdminWhitelistA / isInAdminWhitelistB / canAccessOrgManagement。**依赖来源按 A0 确认结果**：若查库，通过显式传参或注入方式获取 DB，不得自造依赖 |
| `internal/oas/middleware.go` | **全部中间件**（authRequired / adminOnly / ratelimit / 请求日志 / CORS 等，v1.4 补齐），从 main 迁出，禁止散落进各 handler |
| `internal/oas/state.go` | **全部 package 级共享可变状态**（按 A0 `shared-state-inventory.txt` 盘点结果），集中声明，禁止拆散到各文件导致重复声明/行为漂移 |
| `internal/oas/routes.go` | 全部路由注册（package oas，/health、/.well-known/*、/api/v1/auth、/api/v1/owner、/api/v1/admin、/oauth/*、页面路由），从 main 中迁出；挂载时构造 `Handlers` 并注册 |
| `internal/oas/seed.go` | seedRBACPolicies / seedTestUsers / regeneratePolicyCSV 等初始化逻辑（package oas，可引用 model/authz）；**执行时机与拆分前一致（按 A0 seed-behavior.txt）；DB 依赖通过显式传参（如 `seedRBACPolicies(db)`）注入，与 Handlers 同一原则，回报说明（v1.7）** |
| `internal/oas/handlers/auth.go` | auth login/quick-login/dev-token/test-accounts/public-key/roles + oauth 四个端点 handler |
| `internal/oas/handlers/owner.go` | /owner/domains、/owner/policies 相关 handler |
| `internal/oas/handlers/admin_accounts.go` | admin-accounts CRUD + disable/enable/reset-password |
| `internal/oas/handlers/oauth_clients.go` | oauth-clients CRUD + regenerate-secret/suspend/activate |
| `internal/oas/handlers/api_keys.go` | api-keys CRUD + rotate/disable/enable |
| `internal/oas/handlers/federation.go` | federation-nodes CRUD + suspend/activate/trust |
| `internal/oas/handlers/approvals.go` | approvals CRUD + approve/reject/execute |
| `internal/oas/handlers/audit.go` | audit-logs 查询 + 域过滤 |
| `internal/oas/handlers/system_config.go` | system-config / configs / services |
| `internal/oas/handlers/users.go` | users/roles/orgs 管理 + dashboard/stats + ownership/matrix |

**要求**：
- 闭包 handler 改为具名函数；**依赖统一通过一个 `Handlers` struct 注入**（字段以 A0 `deps-inventory.txt` 盘点结果为准，建议含 `DB *gorm.DB; JWT *jwt.Verifier; Log *zap.Logger; Cfg *config.Config` 等），routes 挂载时构造一次，各 handler 文件通过接收者访问依赖，禁止各文件各自造依赖。
- **规模核对（v1.5）**：main.go 按 **7145 行**预估拆分（线上 f7d48aff59 实测为准；A1 开工后先 `wc -l` 实测并回报，**与预估不符以实测为准**，并在拆分计划/回报中说明差异原因）。
- **分域 commit（v1.5）+ 每步可编译纪律（v1.6，红线）**：拆分按域分批提交，建议顺序：`refactor(oas): split model` → `refactor(oas): extract authz/middleware/state` → `refactor(oas): extract seed` → `refactor(oas): extract handlers/auth*` → `refactor(oas): extract handlers/admin* 各域` → `refactor(oas): extract handlers/oauth` → `refactor(oas): extract routes`；**每个 handler 拆出的 commit 必须同步把 main.go 中对应路由注册行改为引用 handlers 包（`r.GET("/x", handlers.XXX)`）**，闭包移走而路由行不跟随 = 中间 commit 编译失败；**每批 commit 提交前 `go build ./...` 必须绿，否则该 commit 禁止提交**。routes.go 在最后一个 commit 统一收走 main 中剩余路由注册行，main 只剩启动。
- **包依赖单向**：`cmd/oas → routes → handlers → model/authz → pkg → 标准库`，禁止反向引用；**先跑通 `go build ./...` 再继续**。
- **git mv 保留历史**：拆分必须用 `git mv` + 块剪切，让 Git rename detection 生效、review 可见新旧对应；**禁止 copy+delete**（整个文件变新增，diff 全红无法 review）。
- **出口标准（写死）**：拆分后 `cmd/oas/main.go` **≤ 150 行**（只剩启动 + 优雅关闭 + 挂载），超过即视为未拆完；配置加载逻辑与启动同处（main 或 `internal/oas/server.go`，二选一并在回报说明）。
- **不改变任何路由路径、HTTP 状态码、响应 JSON 结构**。

### A2 前端模板抽离（14 个 PageHTML → go:embed）
- 将 `loginPageHTML / userMgmtPageHTML / consoleHomePageHTML / overviewPageHTML / approvalsPageHTML / ownershipPageHTML / adminAccountsPageHTML / systemConfigPageHTML / apiKeysPageHTML / federationNodesPageHTML / oauthClientsPageHTML / auditLogsPageHTML / roleMgmtPageHTML / orgMgmtPageHTML` 从 main.go 迁出。
- 落地为 **`internal/oas/handlers/frontend/*.html`（一个页面一个文件），页面渲染函数与 `go:embed` 声明放 handlers 包内**（handler 直接渲染返回；勿放 package oas，避免与 routes 的包关系混乱）。
- **注入方式（二选一，在回报中说明选择）**：
  - **推荐**：保留现有拼接/替换语义——模板文本放文件，运行时用 `strings.Replace` 注入变量。**占位符必须用显式标记（如 `__TOKEN__ / __USERNAME__ / __API_BASE__ / __CAN_WRITE__ / __IS_OUAU__`），禁止用 %s**（HTML/JS 中 % 字面量多，易误替换），**不使用 text/template**，彻底规避转义差异与 JS 中 `{{`/`}}` 字面量冲突；
  - 若选用 text/template：必须显式处理转义差异（HTML 转义 vs 原样拼接）与模板字面量冲突，并逐页做等值验证。
- **注入变量以 A0 `inject-vars-before.txt` 为对照逐页核对**，漏一个占位符即返工。
- **验收基准**：以 A0 `pages-before/` 截图为对照，**静态部分一致 + 注入点行为一致**（页面标题、JS 变量注入、功能按钮可点、console 零新增报错）。

### A3 services/ams 孤儿归档
- `ziway-backend/services/ams/`（独立 go.mod 的第二套实现）**冻结归档**，方式**二选一写死（不再留模糊）**：
  - **方案 A（推荐）**：移动到**仓库根 `archive/ams-20260908/`**（必须在 `ziway-backend/` 之外，避免嵌套独立 go.mod 干扰 `go build ./...`），或
  - **方案 B**：打独立 branch `archive/ams-v1`，并从主分支移除该目录。
- 确认根仓库无任何引用（go.mod require / import / Makefile / scripts / docker-compose）；归档后主树不再包含该模块，避免双轨维护。

### A4 dist/ 二进制出库（先验证部署兼容，再出库）
- **第一步（先做，双轨确认）**：**窗口侧**回报构建入口 / entrypoint 配置 / 部署依赖信息（是否依赖 Git 内 dist/oas）；**先确认 `scripts/build.sh` 是否存在且产出 `dist/oas`（v1.6）**——存在则按脚本实跑验证，**不存在则按等价 `go build` 命令实跑验证并回报，验收不卡在不存在的脚本上**；**主 Agent 侧**同步核对历史部署记录（OAS-R4/R5 预编译方案、deployHistoryId 等）。若部署依赖 Git 内 dist/oas，先给出兼容方案：由 `scripts/build.sh` 在部署时产出 dist/oas，或线上已有产物可延续使用；**未双方确认兼容前不得 `git rm`**。
- **第二步（确认后）**：`git rm --cached ziway-backend/dist/oas dist/ms dist/os`，**同步修改 `.gitignore` 移除 dist 的例外规则（`!dist/` 等，防止出库后又被加回）**，保留 `dist/.gitkeep`（或构建产出说明）。
- 目标：仓库瘦身（约 397MB → <30MB），release 产物改由构建流程产出，不再入库。
- 回报必须说明：出库后部署兼容方案 + 线上部署路径已验证；**且必须实际执行 `scripts/build.sh`（或等价 `go build`）产出 `dist/oas` 并本地启动验证（能起服务、`/health` 200），禁止只回报方案不实跑（v1.5）**。

## 验收标准（双轨：窗口自测对照 A0 快照 → 主 Agent 部署后线上复测）
| # | 用例 | 期望（窗口自测 → 主 Agent 复测） |
|---|---|---|
| 0 | A0 基线产物提交（routes-before / 脱敏快照（14 资源域逐域打钩）/ pages-before / inject-vars / deps / shared-state / seed-behavior / 编译输出）+ 独立分支 `refactor/oas-baseline` | 已提交、已推分支、可对照 |
| 1 | 开工检查：`git status` 工作区干净（未提交改动已提交/stash）+ **窗口回报已收到 08 暂停指示、无 08 未提交改动（v1.7）** | 已确认 |
| 2 | 编译与静态检查：`go build ./...` + `go vet ./...` + `go test ./...` | 零错误零告警、测试全绿（与 A0 基线一致，证明无循环依赖） |
| 3 | 路由一致性：拆分后路由表与 A0 `routes-before.txt` 逐条对照（engine.Routes 导出） | 全量一致 → 主 Agent 复核 |
| 4 | 接口回归：按 A0 口径分层比对（结构逐接口 / 值只对幂等接口 / 业务列表只对结构），**14 资源域逐域对照** | 一致 → 主 Agent 线上复测 |
| 5 | **权限矩阵回归**：SU 可操作项 200、OAM 治理明细（audit-logs/oauth-clients 等）403、匿名 401、**禁用账号登录 401（以 A0 快照固化拆分前线上实际行为为准，与 OAS-CONSOLE-08 口径统一，禁止 401/403 漂移）**。**线上复测：主 Agent 全量复测（复用 08 测试脚本：9 项基础 + 权限矩阵 + OAuth 客户端链路），不抽样**。**断言冲突裁决（v1.6）**：08 脚本断言与 A0 快照冲突时，**以 A0 快照（拆分前线上实际行为）为准并记录差异，禁止按 08 预期改 09 行为** | 权限语义与拆分前完全一致 |
| 6 | 页面回归：14 个页面与 A0 截图对比（静态一致 + 注入点行为一致 + console 零新增报错 + 注入变量对照 inject-vars-before.txt） | 窗口截图自测 → 主 Agent 复核 |
| 7 | **seed 回归**：种子数据一致（test-accounts 5 账号、oas-ou-owner 等）+ **执行时机与拆分前一致（对照 seed-behavior.txt）** + **seed DB 依赖注入方式已回报（v1.7）** | 一致 |
| 8 | services/ams 归档：主树无引用、归档位置/branch 明确（按 A3 方案 A/B）。**判定节点：A3 完成后（⑦），不在窗口自测阶段（v1.6）** | 完成 |
| 9 | dist 出库：部署兼容方案双方确认 + Git 不再跟踪三个二进制 + .gitignore 已移除例外，仓库体积下降。**判定节点：A4 完成后（⑧），不在窗口自测阶段（v1.6）** | 完成 → 主 Agent 线上部署路径验证 |
| 10 | 构建脚本兼容：Makefile / scripts/build.sh / run.sh 在拆分后可用（或已同步更新并在回报说明） | 可用 |
| 11 | 提交规范：**git mv 拆分（历史可追溯）**，每个拆分任务一个 commit（如 `refactor(oas): split model` / `refactor(oas): extract handlers/*`），**每批 commit 前 go build 必须绿（v1.6）**，推送到 `Yorkzhao8888/ziway-oas` | commit 清晰可 review、每步可编译 |
| 12 | 出口标准：`cmd/oas/main.go` ≤ 150 行（配置加载/中间件/state 已全部迁出） | 达标 |
| 13 | 回滚预案（**双锚点实体化，v1.7**）：中间部署前异常回滚 f7d48aff59；中间部署验证通过后打 tag `oas-09-mid-verified`，A3/A4 异常回滚该 tag（保留拆分成果、线上不回退）；A0 基线可从 `refactor/oas-baseline` 恢复 | 主 Agent 确认 |
| 14 | **中间部署验证**：A1+A2 窗口自测通过后，主 Agent 中间部署一次 + 线上复测（纯拆分零变化验证：路由一致、权限矩阵一致、页面一致、seed 一致、禁用账号 401），**通过后立即打 tag `oas-09-mid-verified`（v1.7）** | 主 Agent 完成并回报 |
| 15 | **最终部署→线上复测→关单**：A3/A4 确认后，**主 Agent 先在部署环境用新构建路径 dry-run（构建 → 起服务 → /health 200）通过（v1.7）**，再最终部署 → 线上全量复测（9 项基础 + 权限矩阵 + OAuth 客户端链路）全部通过 → 关单 | 主 Agent 完成并回报 |

## 约束
- **只拆不重构**：禁止顺手优化逻辑、改命名规范、调整响应结构、增删功能。本单是机械拆分，任何"顺带改进"另开单。
- **与 08 时序（v1.5）**：09 开工前置 = 窗口已回报"收到 08 暂停指示、无 08 未提交改动"（v1.7 强化）；08 收窄在 09 拆分部署后于结构化代码上实施。若用户改判 08 先行，本单基线/回滚点随新 HEAD 后移。
- **断言冲突裁决（v1.6）**：复测时 08 测试脚本断言与 A0 快照冲突，**以 A0 快照（拆分前线上实际行为）为准**，禁止按 08 预期改 09 行为。
- **README / AGENTS.md / API_MAPPING.md 文档更新不入本单**（拆分后目录结构变化导致的文档过时，统一在 B 阶段处理，避免范围蔓延）。
- **线上行为零变化**：拆分完成后接口、页面、权限语义与拆分前完全一致（以 A0 基线 + 线上域 62j75kfyn3.coze.site 现状为准）。
- **包依赖单向**：cmd/oas → routes → handlers → model/authz → pkg → 标准库，禁止反向引用；违反即循环导入编译失败。
- **快照脱敏**：任何快照产物不得包含有效 token/密码/密钥原文。
- **git mv 拆分**：禁止 copy+delete，保证 rename 历史与 review 可追溯。
- **回滚优先**：任何一步出现异常（编译失败/回归不符/线上异常），立即停止并向主 Agent 报告，按双锚点规则回滚，禁止在异常态继续叠加修改。
- 数据库/部署形态（SQLite vs PG、单副本 vs 多副本）**不在本单范围**，B 阶段另行裁定。
- 部署权归主 Agent：窗口只回报 commitHash + 自测结果（编译/路由对照/权限矩阵/页面截图/部署兼容说明），**不自行部署线上**。
